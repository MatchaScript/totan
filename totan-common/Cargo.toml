[package]
name = "totan-common"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[features]
ebpf = []

[dependencies]
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }
url = { workspace = true }
